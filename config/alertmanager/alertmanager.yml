global:
  resolve_timeout: 5m

route:
  receiver: local-null
  group_by: ["alertname", "project", "environment"]
  group_wait: 20s
  group_interval: 2m
  repeat_interval: 2h

receivers:
  - name: local-null

inhibit_rules: []
